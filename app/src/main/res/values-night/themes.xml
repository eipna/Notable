<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Notable" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Changes color of status bar -->
        <item name="android:statusBarColor">@color/primary</item>

        <!-- Changes color of preference category title text -->
        <item name="colorAccent">@color/ghost_white</item>

        <!-- Sets a custom theme for the overflow button -->
        <item name="actionOverflowButtonStyle">@style/OverFlowButtonStyle</item>

        <!-- Sets a the icon for the home as up button in toolbar -->
        <item name="homeAsUpIndicator">@drawable/arrow_back</item>
    </style>

    <style name="Theme.Notable" parent="Base.Theme.Notable" >
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Custom theme for popup menus -->
    <style name="PopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="popupMenuBackground">@drawable/popup_menu</item>
        <item name="android:textColor">@color/popup_menu_text</item>
    </style>

    <!-- Custom theme/color for kebab/more options icon -->
    <style name="OverFlowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/primary_invert</item>
    </style>

    <!-- Custom style for collapsed toolbar -->
    <style name="CollapsedToolbarStyle">
        <item name="android:textSize">37sp</item>
    </style>

    <!-- Custom style for toolbar drop shadow -->
    <style name="DropShadowStyle">
        <item name="android:background">@drawable/drop_shadow</item>
        <item name="android:visibility">invisible</item>
    </style>

    <!-- Custom configuration for recycler views -->
    <style name="RecyclerStyle">
        <item name="android:layout_marginEnd">6dp</item>
        <item name="android:layout_marginStart">4dp</item>
    </style>

    <!-- Custom global colors -->
    <color name="primary">@color/dark_grey</color>
    <color name="primary_invert">@color/ghost_white</color>

    <color name="popup_menu_bg">#292929</color>
    <color name="popup_menu_text">#E1E1E1</color>

    <color name="FAB_bg">#3C3C3C</color>
    <color name="FAB_text">#BFBFBF</color>
</resources>